import { OperationDirective } from './operation.directive';

describe('OperationDirective', () => {
  it('should create an instance', () => {
    const directive = new OperationDirective();
    expect(directive).toBeTruthy();
  });
});
